// Placeholder file - tidak digunakan
exports.handler = async function(event, context) {
  return {
    statusCode: 200,
    body: JSON.stringify({ message: "Aplikasi ini sekarang sepenuhnya client-side dan tidak memerlukan functions" })
  };
};
